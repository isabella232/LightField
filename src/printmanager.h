#include <QProcess>

class PrintManager: public QObject {

    Q_OBJECT

public:

    PrintManager( QObject* parent = 0 );
    virtual ~PrintManager( ) override;

protected:

private:

signals:

public slots:

protected slots:

private slots:

};
