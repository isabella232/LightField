#ifndef __PREPARETAB_H__
#define __PREPARETAB_H__

class PrepareTab: public QWidget {

    Q_OBJECT

public:

    PrepareTab( QWidget* parent = nullptr ): QWidget( parent ) {
        /*empty*/
    }

    virtual ~PrepareTab( ) override {
        /*empty*/
    }

protected:

private:

signals:

public slots:

protected slots:

private slots:

};

#endif // __PREPARETAB_H__
